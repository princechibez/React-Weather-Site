import React from "react";

const errPage = (props) => (
    <div style={{textAlign: "center", fontSize: "50px", color: "white"}}>
        <h1>Page not found!!!</h1>
    </div>
);

export default errPage;